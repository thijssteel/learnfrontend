// @flow
import {  } from './accountActions'

const initialState = {
  token: '',
}

export default function accountReducer (state= initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}